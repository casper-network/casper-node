{
  "scripts": {
    "asbuild:optimized": "asc --lib ../../.. assembly/index.ts -b ../../../../target-as/transfer_purse_to_account_stored.wasm   --optimize --use abort=",
    "asbuild": "npm run asbuild:optimized"
  },
  "devDependencies": {
    "assemblyscript": "^0.8.1"
  }
}
